name: CloudCommerce CI/CD

on:
  push:
    branches:
      - main

  pull_request:
    branches:
      - main

jobs:

  frontend:
    name: Frontend Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install Frontend Dependencies
        working-directory: apps/frontend
        run: npm ci

      - name: Build Frontend
        working-directory: apps/frontend
        run: npm run build

  backend:
    name: Backend Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Backend Dependencies
        working-directory: apps/product-service
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

  docker:
    name: Build & Push Docker Images

    needs:
      - frontend
      - backend

    runs-on: ubuntu-latest

    permissions:
      contents: read

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_TOKEN }}

      - name: Build and Push Backend Image
        uses: docker/build-push-action@v6
        with:
          context: ./apps/product-service
          push: true
          platforms: linux/amd64,linux/arm64
          tags: |
            mansha716/cloudcommerce-backend:latest
            mansha716/cloudcommerce-backend:${{ github.sha }}

      - name: Build and Push Frontend Image
        uses: docker/build-push-action@v6
        with:
          context: ./apps/frontend
          push: true
          platforms: linux/amd64,linux/arm64
          tags: |
            mansha716/cloudcommerce-frontend:latest
            mansha716/cloudcommerce-frontend:${{ github.sha }}
